import { BigLetterDirective } from './big-letter.directive';

describe('BigLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new BigLetterDirective();
    expect(directive).toBeTruthy();
  });
});
